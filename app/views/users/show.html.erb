<div class="row">
  <aside class="col-md-4">
    <section class="stats">
      <%= render 'shared/stats' %>
    </section>
  </aside>
  <div class="col-md-8">
    <%= render 'followform' if logged_in? %>
  </div>
</div>

<%= link_to 'logout', logout_path, method: :delete %>